// Import from registry singleton for re-export
import { getCmsComponentRegistry } from "./src/registry";

// Export types
export type * from "./src/types";

// Re-export registry singleton
export { getCmsComponentRegistry };
